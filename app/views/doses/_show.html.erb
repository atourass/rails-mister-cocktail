<ul class="list-group">
   <% doses.each do |dose| %>
  <li class="list-group-item">
    <%= "#{dose.description} of #{dose.ingredient.name}" %>
    <%= link_to "Delete", dose_path(dose.id), data: {:confirm => 'Are you sure?'}, :method => :delete %>
  </li>
  <% end %>
</ul>
